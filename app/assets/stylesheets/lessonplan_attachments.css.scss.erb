@import "base";

#attachments {
  section.info {
    overflow: hidden;
    position: relative;
    padding: 10px 0;

    b {
      position: absolute;
      top: 50%;
      right: 200px;
      margin-top: -.5em;
      color: $font-light-color;
    }

    .btn-primary {
      padding: 2px 6px !important;
      margin-right: 10px !important;
      float: right;
    }
  }
}

.attachment-list {
  list-style: none;
  overflow: hidden;
  background: whiteSmoke;
  padding: 5px 10px;

  $max-height: 150px;

  li {
    float: left;
    margin: 10px 5px;
    position: relative;
  }

  a {
    display: block;
    height: 150px;

    img {
      max-height: $max-height;
      border: 2px solid white;
      @include box-shadow(0 2px 5px #999);
    }
  }

  button {
    position: absolute;
    top: -7px;
    right: -7px;
    display: none;
    cursor: pointer;
    @include background(linear-gradient(top, #D36B78, #BB1F23));
    color: white;
    border: 2px solid white;
    height: 30px;
    width: 30px;
    border-radius: 30px;
    z-index: 1;
    border: 2px solid white;
    @include box-shadow(0 0 10px #AAA);
    font-size: 28px;
  }

  li:hover button {
    display: block;
  }
}

.scratch-attachment-list{
  li{
    list-style-type: none;
    position: relative;
    float: left;
    width: 16%;
    height: 130px;
    margin: 0 20px 40px 0;

    a{
      span{
        position: absolute;
        top: 100%;
      }
    }
  }

  li a:before{
    font-family: 'fontello';
    display: block;
    position: absolute; left: 0;
    text-align: center;
    width: 100%;
    height: 125px;
    font-size: 70px;
    line-height: 125px;
    content: '✒';
    background: #61b1bd;
    color: #fff;
  }

  <%
  lessonplan_icons = %w[  📣 ♥ 🔆 ⚒ 🔔 ⏳ ];
  lessonplan_icon_bg = %w[#FE7B9D #37B4AE #389ABD #FBB963 #D52A24 #E0DFC2 #F7E9A7 #A7D4C0 #E49996]
  5.times do |time|
      lessonplan_icons.each_with_index do |icon, index|
      li_str = Array.new(time * 9 + index){ "+li" };
      %>li<%=li_str.join('')%> a:before{ content: "<%= icon%>"; background: <%=lessonplan_icon_bg[index] %> }<%
      end
  end
  %>
}

.scratch-dialog-box.dialog{
  width: 700px;
  height: 600px;
}

ul.fileList {
  padding: 10px 0;
  margin: 0 0 10px;
  overflow: auto;
  /*height: 400px;*/

  li {

    $bottom-gap: 20px;
    $file-item-height: 100px;

    list-style: none;
    float: left;
    margin: 0 5px 10px 0;
    background: #eee;
    padding: 5px;
    width: 22%;
    height: $file-item-height;
    position: relative;
    @include box-shadow(inset 0 0 20px #ccc);

    .file {
      display: none;
      width: 100%;
    }

    b.file-name {
      font-size: 12px;
      font-weight: normal;
      color: #888;
      width: 100%;
      height: 30px;
      word-wrap: break-word; /* IE 5.5-7 */
      white-space: -moz-pre-wrap; /* Firefox 1.0-2.0 */
      white-space: pre-wrap; /* current browsers */
    }

    .progressbar {
      display: block;
      position: absolute;
      bottom: 10px;
      left: 10px;
      right: 40px;
      height: 6px;
      border: 1px solid #4C88BC;
      background: #fff;
      @include box-shadow(inset 1px 1px 2px #ddd);
      @include border-radius(6px);

      .progress {
        position: absolute;
        top: 0;
        bottom: 0;
        left: 0;
        width: 0%;
        background: #4C9ED0;
        @include background(linear-gradient(top, #37AAEA, #117ED2));
        @include border-radius(4px);
      }

      .progress-value{
        position: absolute;
        right: -30px;
        top: -2px;
        font-size: 10px;
        color: #888;
      }
    }
  }
}